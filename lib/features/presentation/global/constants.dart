import 'package:flutter/animation.dart';

const kDineBottomNavigationBarHeight = 84.0;

const kDineButtonHeight = 68.0;

const kDineToolbarHeight = 96.0;

const kPageAnimationDuration = Duration(milliseconds: 250);

const kResizeAnimationCurve = Interval(0.4, 1.0, curve: Curves.ease);
